import 'package:flutter/material.dart';

class ProdLineResearch extends StatelessWidget {
  ProdLineResearch({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.spaceEvenly,
      children: <Widget>[
        Text('RESEARCH'),
      ],
    );
  }
}
