import 'package:flutter/material.dart';

class ProdLineTeams extends StatelessWidget {
  ProdLineTeams({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisAlignment: MainAxisAlignment.spaceEvenly,
      children: <Widget>[
        Text('TEAMS'),
      ],
    );
  }
}
